# Sample PDFs for TaxMaxx Backend Testing

Place local parser test files here, for example:
- `w2_sample_01.pdf`
- `1098t_sample_01.pdf`

This folder is intentionally empty in git (except `.gitkeep`) so teammates can add/remove local PDFs without changing backend code.

Do not commit real personal tax documents or any sensitive data.
